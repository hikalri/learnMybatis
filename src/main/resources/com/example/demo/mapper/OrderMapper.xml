<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OrderMapper">
    <cache type="org.mybatis.caches.ehcache.EhcacheCache" />

    <!-- 结果映射 -->
    <resultMap id="orderResultMap" type="Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="amount" column="amount"/>
        <result property="userId" column="user_id"/>
        <result property="orderTime" column="order_time"/>
    </resultMap>

    <!-- 带用户关联的结果映射（多对一关系） -->
    <resultMap id="orderWithUserResultMap" type="Order" extends="orderResultMap">
        <association
            property="user"
            resultMap="com.example.demo.mapper.UserMapper.userResultMap"
            columnPrefix="user_"
        >
        </association>
    </resultMap>

    <!-- 根据ID查询订单 -->
    <select id="selectById" resultMap="orderResultMap">
        SELECT id, order_no, amount, user_id, order_time
        FROM orders
        WHERE id = #{id}
    </select>

    <!-- 查询所有订单 -->
    <select id="selectAll" resultMap="orderResultMap">
        SELECT id, order_no, amount, user_id, order_time
        FROM orders
    </select>

    <!-- 插入订单 -->
    <insert id="insert" parameterType="Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_no, amount, user_id)
        VALUES (#{orderNo}, #{amount}, #{userId})
    </insert>

    <!-- 更新订单 -->
    <update id="update" parameterType="Order">
        UPDATE orders
        SET order_no = #{orderNo},
            amount = #{amount},
            user_id = #{userId}
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除订单 -->
    <delete id="deleteById">
        DELETE FROM orders WHERE id = #{id}
    </delete>

    <!-- 根据用户ID查询订单（一对多关系） -->
    <select id="selectByUserId" resultMap="orderResultMap">
        SELECT id, order_no, amount, user_id, order_time
        FROM orders
        WHERE user_id = #{userId}
    </select>

    <!-- 根据ID查询订单及其关联的用户（多对一关系） -->
    <select id="selectWithUser" resultMap="orderWithUserResultMap">
        SELECT o.id, o.order_no, o.amount, o.user_id, o.order_time,
               u.id as user_id, u.name as user_name, u.email as user_email,
               u.age as user_age, u.department_id as user_department_id
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.id
        WHERE o.id = #{id}
    </select>

    <!-- 查询所有订单及其关联的用户（多对一关系） -->
    <select id="selectAllWithUser" resultMap="orderWithUserResultMap">
        SELECT o.id, o.order_no, o.amount, o.user_id, o.order_time,
               u.id as user_id, u.name as user_name, u.email as user_email,
               u.age as user_age, u.department_id as user_department_id
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.id
        ORDER BY o.id
    </select>

</mapper>